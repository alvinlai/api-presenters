require 'spec_helper'

describe "API errors" do
  it "should return record not found for nonexistent records" do
    pending
    #get :show, :id => 0
    #error = { :error => 'record not found' }
    #response.body.should eql(error.to_json)
    #response.status.should eql(404)
  end
end
